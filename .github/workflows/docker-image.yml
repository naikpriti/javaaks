name: java demp app
on: [push]

# Environment variables available to all jobs and steps in this workflow
env:
  REGISTRY_NAME: demoaksnov
  CLUSTER_NAME: tstk8cluster
  CLUSTER_RESOURCE_GROUP: tstk8grp
  NAMESPACE: dev
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    # Connect to Azure Container registry (ACR)
    - uses: azure/docker-login@v1
      with:
        login-server: ${{ env.REGISTRY_NAME }}.azurecr.io
        username: ${{ secrets.REGISTRY_USERNAME }} 
        password: ${{ secrets.REGISTRY_PASSWORD }}
    
    # Container build and push to a Azure Container registry (ACR)
    - run: |
        docker build . -t ${{ env.REGISTRY_NAME }}.azurecr.io/javaimage:${{ github.sha }}
        docker push ${{ env.REGISTRY_NAME }}.azurecr.io/javaimage:${{ github.sha }}
